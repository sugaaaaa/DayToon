<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DayToon"
        tools:targetApi="31">
        <activity
            android:name=".controller.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.IndexActivity" />
        <activity android:name=".controller.activity.LoginActivity" />
        <activity android:name=".controller.activity.SingUpActivity" />
        <activity android:name=".controller.activity.SingOutActivity" />
        <activity android:name=".controller.activity.TypeStoryActivity" />
        <activity android:name=".controller.activity.WelcomePageActivity" />
        <activity android:name=".controller.activity.Welcome2Activity" />
        <activity android:name=".controller.activity.StoryShowActivity"/>
        <activity android:name=".controller.activity.secretelove.ChapterActivity"/>
        <activity android:name=".controller.activity.secretelove.ComicsReadActivity"/>
    </application>

</manifest>